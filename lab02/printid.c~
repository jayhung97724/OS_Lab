#include <sys/types.h>
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

int main(void) {
	seteuid(0);
	printf(
		"Real      UID = %d\n"
		"Effective UID = %d\n",
		getuid(),
		geteuid()	
	);
	return 0;
}
